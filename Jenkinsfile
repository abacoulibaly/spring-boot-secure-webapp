
pipeline {
 stages {
        stage('without-docker ') {
            steps {
                sh '''
                      echo " echo without Docker !"
                           }
                                 }
	   
	        }
	   }
